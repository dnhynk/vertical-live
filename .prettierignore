node_modules
**/dist
package-lock.json

# Generated from the zod schemas by `npm run schema:generate -w @vl/contract`
# and never hand-edited (CLAUDE.md §4). Byte-for-byte freshness is enforced by
# packages/contract/src/schema/registry.test.ts, not by a formatter.
packages/contract/schema

# Prototype snapshot kept for reference only (spec §10.4). Not on the product path.
legacy

# Markdown is authored by hand (spec/BOARD/tickets are the canonical documents and
# BOARD.md is committed to main by the coordinator). Doc formatting is decided in T16.
**/*.md
